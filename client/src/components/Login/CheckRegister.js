import React from 'react';
import { NavLink } from 'react-router-dom';

const CheckRegister = () => {
    return (
        <div>
            Se registro correctament
            <p>saduasdyhi</p>
            <button><NavLink to='/login'>Iniciar</NavLink></button>
        </div>
    );
};

export default CheckRegister;